# 核心依赖
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
joblib>=1.3
python-docx>=1.1

# 分词（FengCi0 特征工程需要）
jieba==0.42.1

# 深度学习（HC3 引擎需要，可选）
torch>=2.0
sentence-transformers>=2.2

# HTTP 客户端（MiMo API / Binoculars API 需要）
httpx>=0.24

# 配置文件（可选）
pyyaml>=6.0
